let a = 1;
var b;
let c = a ? b : false;

console.log("hello world" + c);

const pi = 2.14 + 1;

function move(
    dir,
    coordinates,
    steps
) {
    return x + 1;
}

var obj = {
    name : "str",
    age : 12,
    another_obj : {
        call: false,
        num : 123,
        string: "str-2$",
        another_obj_obj : {
            key : "found"
        }
    }
};

console.log(obj.another_obj.another_obj_obj.key);

delete obj.another_obj;

switch(expression) {
    case x:
      // code block
      if (x < 13) {
        try {
            while(x < 0 && x !== null) {
                for (let i = 0; i < 3; ++i) {
                    i++;
                    break;
                }
                if (x != true) {
                    // continue;
                    // skip 1 iteration
                } else {
                    console.log (hello + arr + "");
                }
            } 
        } catch (err) {
            throw (err);
            while (x < 1) {
                if (x == true) {
                    // continue;
                }
            }
        } 
      } else {
        break;
      } 
      break;
    case y:
      // code block
      console.log(x);
      break;
    default:
        // continue;
        console.log(test);
    // code block
  }