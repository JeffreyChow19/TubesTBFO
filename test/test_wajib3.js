let a = 1;
let b;
let c = a ? b : false;

console.log("hello world" + c);

const pi = 2.14 + 1;


switch(expression) {
    case x:
      // code block
      break;
    case y:
      // code block
      break;
    default:
      // code block
  }